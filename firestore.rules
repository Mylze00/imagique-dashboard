rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // ✅ Autoriser seulement les utilisateurs authentifiés à lire/écrire
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}